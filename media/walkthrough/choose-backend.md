# Freebird Cloud (default)

Works instantly with **20 free AI edits per day** — no API key, no local model to install.

Want something different?

- **Ollama** — unlimited, 100% private, runs on your machine
- **Bring your own key** (Pro) — Claude, GPT-4o, DeepSeek, or Qwen

You can change this later anytime from `Freebird: Configure AI Backend`.
