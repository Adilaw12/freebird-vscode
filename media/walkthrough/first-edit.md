# Two ways to use Freebird

**Chat sidebar** — ask questions, request multi-file changes, or run `/commit`
to generate a commit message from your staged changes.

**Inline edit** — select code, press `Ctrl+Alt+K` (`Cmd+Alt+K` on Mac),
describe the change, and Freebird edits it in place.
